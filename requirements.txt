# requirements.txt for Job Apply Bot
# These are the Python dependencies you need to install before running job_apply_bot.py

# Core automation
playwright>=1.47.0

# Data handling
pandas>=2.2.2

# Google Sheets integration
gspread>=6.0.0
google-auth>=2.33.0

# Resume parsing and regex extraction
PyPDF2>=3.0.1
validators>=0.24.0

# Environment variable management
python-dotenv>=1.0.1

# Optional schema validation (if you want to extend data models)
pydantic>=2.9.2

# Additional helper for reading .env and secure storage (optional)
cryptography>=43.0.3

bcrypt>=4.1.2
validators>=0.24.0
flask>=3.0.3
# To install all:
# pip install -r requirements.txt

# After installing, also run once:
# playwright install
